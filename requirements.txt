# 测试框架
pytest>=7.0
pytest-html>=4.0

# 配置与网络
pyyaml>=6.0
requests>=2.28
python-dotenv>=1.0

# 设备连接
pymodbus>=3.5          # ModbusTCP 连接
pyvisa>=1.13           # VISA 连接（GPIB/USB）
pyvisa-py>=0.5         # VISA 纯 Python 后端（免装 NI-VISA）

# 数据处理与导出
pandas>=2.0            # 数据分析
openpyxl>=3.1          # Excel 导出
jinja2>=3.1            # 报告模板渲染
